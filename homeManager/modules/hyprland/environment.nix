''
  -- Environment and rendering
  hl.env("HYPRCURSOR_THEME", "rose-pine-hyprcursor")
  hl.env("HYPRCURSOR_SIZE", "24")

  hl.config({
    render = {
      cm_enabled = true,
      cm_auto_hdr = 1,
    },

    quirks = {
      prefer_hdr = 1,
    },
  })
''
